name: ebury_challenge

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
analysis-paths: ["analysis"]
macro-paths: ["macros"]

models:
  ebury_challenge:
    aggregates:
      schema: analytics
      materialized: table
    marts:
      schema: marts
      materialized: table
    clean:
      schema: clean
      materialized: table
